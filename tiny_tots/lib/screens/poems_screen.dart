import 'package:flutter/material.dart';

class PoemsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Poems")),
      body: Center(child: Text("Poems Content Coming Soon!")),
    );
  }
}

import 'package:flutter/material.dart';

class ColorsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Colors")),
      body: Center(child: Text("Colors Content Coming Soon!")),
    );
  }
}

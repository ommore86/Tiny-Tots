import 'package:flutter/material.dart';

class AnimalsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Animals")),
      body: Center(child: Text("Animals Content Coming Soon!")),
    );
  }
}

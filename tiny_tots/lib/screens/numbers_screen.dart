import 'package:flutter/material.dart';

class NumbersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Numbers")),
      body: Center(child: Text("Numbers Content Coming Soon!")),
    );
  }
}

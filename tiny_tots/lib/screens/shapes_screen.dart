import 'package:flutter/material.dart';

class ShapesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Shapes")),
      body: Center(child: Text("Shapes Content Coming Soon!")),
    );
  }
}

import 'package:flutter/material.dart';

class BirdsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Birds")),
      body: Center(child: Text("Birds Content Coming Soon!")),
    );
  }
}
